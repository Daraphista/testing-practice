import reverseString from "../code/reverseString.js";

test("reverses string to gnirts", () => {
  expect(reverseString("string")).toBe("gnirts");
});
test("reverse word to drow", () => {
  expect(reverseString("word")).toBe("drow");
});
